name: Rust CI (Smart Contracts)
on: [push, pull_request]

jobs:
  rust-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

      - name: Run Rust tests
        run:  Node CI workflow

          echo "No Rust tests yet" && exit 0
           

